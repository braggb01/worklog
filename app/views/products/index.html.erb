<div class="row">
    <div class="span12 vspacerf">
        <h3 style="display:inline;">Products</h3>
        <div class="btn-group pull-right">
            <button class="btn btn-success"><i class="icon-cog"></i> Actions</button>
            <button class="btn btn-success dropdown-toggle" data-toggle="dropdown">
                <span class="caret"></span>
            </button>
            <ul class="dropdown-menu">
                <li><%= link_to 'New Product', new_product_path %></li>
            </ul>
        </div>
    </div>
</div>
<div class="row">
    <div class="span12 vspacerf">
        <% if @products.count < 1 %>
            <h3>You have not entered any Products yet.  <%= link_to 'Enter One Now', new_product_path %></h3>
        <% else %>
            <table class="table">
                <thead>
                    <th>Vendor</th>
                    <th>Part Number</th>
                    <th>Action</th>
                </thead>
                <tbody>
                    <%= render @products %>
                </tbody>
            </table>

            <%= will_paginate %>
        <% end %>
    </div>
</div>