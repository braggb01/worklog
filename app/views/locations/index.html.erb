<div class="row">
    <div class="span12 vspacerf">
        <h3 style="display:inline;">Locations</h3>
        <div class="btn-group pull-right">
            <button class="btn btn-success"><i class="icon-cog"></i> Actions</button>
            <button class="btn btn-success dropdown-toggle" data-toggle="dropdown">
                <span class="caret"></span>
            </button>
            <ul class="dropdown-menu">
                <li><%= link_to 'New Location', new_location_path %></li>
            </ul>
        </div>
    </div>
</div>
<div class="row">
    <div class="span12 vspacerf">
        <% if @locations.count < 1 %>
            <h3>You have not entered any Locations yet.  <%= link_to 'Enter One Now', new_location_path %></h3>
        <% else %>
            <table class="table">
                <thead>
                    <th>Address</th>
                    <th>Action</th>
                </thead>
                <tbody>
                    <%= render @locations %>
                </tbody>
            </table>

            <%= will_paginate %>
        <% end %>
    </div>
</div>